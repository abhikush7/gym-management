{
  "name": "ironforge-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for IronForge",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.6.0",
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/nodemailer": "^6.4.14",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": "18"
  }
}
